import asyncio
import json
from dealhunter.providers.uber_eats.browser_transport import UberBrowserTransport

# We need UUIDs of restaurants.
# Let's search for restaurants using browser transport?
# We can just fetch known URLs or UUIDs. Wait, how do I find restaurant UUIDs?
