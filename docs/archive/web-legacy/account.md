# Web Account Diagnostics

Diagnostic interface to view account status without exposing credentials. It uses `DealHunter`'s Account module to test the configured session token safely against provider APIs.
